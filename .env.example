# ============================================================
# Outbound.AI - Environment Variable Template
# ============================================================
# Copy to `.env` and fill in.

NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# --- Required in every environment ---
DATABASE_URL=postgresql://postgres:root@localhost:5432/outbound_ai
REDIS_URL=redis://localhost:6379

# --- Required in every environment (server refuses to boot if unset) ---
# JWT_SECRET: any high-entropy string, min 32 chars. Rotate with care.
# ENCRYPTION_KEY: hashed to 32 bytes for AES-256. Rotate = must re-encrypt secrets.
JWT_SECRET=
ENCRYPTION_KEY=

# --- Optional: AI features (endpoints return 503 if missing) ---
GEMINI_API_KEY=

# --- Optional: billing ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_FREE_ID=
STRIPE_PRICE_GROWTH_ID=
STRIPE_PRICE_ENTERPRISE_ID=

# --- Optional: Gmail / Outlook mailbox sync ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
